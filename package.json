{
  "name": "11ty-sandbox",
  "version": "2.1.0",
  "main": "",
  "license": "WTFPL",
  "scripts": {
    "build": "eleventy",
    "clean": "rimraf _site",
    "start": "eleventy --serve",
    "debug": "cross-env DEBUG=Eleventy* npx @11ty/eleventy"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.2",
    "rimraf": "^6.0.1"
  },
  "devDependencies": {
    "cross-env": "^10.0.0"
  }
}
